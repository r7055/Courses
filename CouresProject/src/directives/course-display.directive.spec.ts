import { CourseDisplayDirective } from './course-display.directive';

describe('CourseDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
